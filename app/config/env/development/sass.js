/**
 * @config
 * @title sass
 * @package sass gulp-sass
 * @env development
 */

module.exports = {
	settings: {
		errLogToConsole: true,
		outputStyle: 'nested',
		sourceComments: false
	}
};