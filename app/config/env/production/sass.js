/**
 * @config
 * @title sass
 * @package sass gulp-sass
 * @env production
 */

module.exports = {
	settings: {
		errLogToConsole: true,
		outputStyle: 'compressed',
		sourceComments: false,
	}
};