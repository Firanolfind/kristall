/**
 * @title Release
 * @task
 * @name release
 * @description publish Release build version archive ???
 */

const Pump	= require('pump');

module.exports = {
	deps: ['build'],
	fn: function(Gulp, cb){

	}
};