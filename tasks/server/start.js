/**
 * @title Server Start
 * @task
 * @name server:start
 * @description launches server
 */

const Pump	= require('pump');

module.exports = {
	deps: ['build'],
	fn: function(Gulp, cb){

	}
};